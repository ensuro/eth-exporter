# OSV-Scanner configuration
# See https://google.github.io/osv-scanner/configuration/ for the full schema.

# No vulnerabilities are currently ignored.
# Add [[IgnoredVulns]] entries here after the first scan, following the
# `id` / `ignoreUntil` / `reason` schema documented above.
